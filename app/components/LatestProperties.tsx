'use client';
import{useEffect,useState}from'react';const API='https://lbkdoxpanxvsrgqimzzj.supabase.co',KEY='sb_publishable_6uNgrPI-Mj84tlHcFuICTQ_aG2zfoSU';export default function LatestProperties(){const[items,setItems]=useState<any[]>([]);useEffect(()=>{fetch(`${API}/rest/v1/public_properties?select=*&order=created_at.desc&limit=3`,{headers:{apikey:KEY}}).then(r=>r.json()).then(d=>Array.isArray(d)&&setItems(d)).catch(()=>{})},[]);if(!items.length)return null;const money=(p:any)=>p.asking_price?new Intl.NumberFormat('it-IT',{style:'currency',currency:'EUR',maximumFractionDigits:0}).format(p.asking_price)+(p.listing_type==='rent'?' / mese':''):'Prezzo su richiesta';return <section className="home-showcase properties-showcase"><div className="showcase-head"><div><span className="kicker">ULTIMI IMMOBILI PUBBLICATI</span><h2>Le opportunità più recenti.</h2><p>La Home si aggiorna automaticamente quando pubblichi un nuovo immobile dalla Dashboard.</p></div><a className="ghost" href="/immobili">Vedi tutti gli immobili →</a></div><div className="home-property-grid">{items.map(p=><a href="/immobili" className="home-property-card" key={p.id}><div className="home-property-image" style={{backgroundImage:`url(${p.image_urls?.[0]||''})`}}><span>{p.listing_type==='rent'?'IN AFFITTO':'IN VENDITA'}</span></div><div className="home-property-content"><small>{p.municipality} · {p.province}</small><h3>{p.title}</h3><strong>{money(p)}</strong><p>{p.square_meters||'—'} mq · {p.bedrooms||'—'} camere · {p.bathrooms||'—'} bagni</p><span className="home-card-link">Scopri la scheda →</span></div></a>)}</div></section>}