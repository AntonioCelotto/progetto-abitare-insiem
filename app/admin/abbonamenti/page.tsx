'use client';import{useEffect,useState}from'react';import AdminGuard from'../AdminGuard';import'../admin.css';const API='https://lbkdoxpanxvsrgqimzzj.supabase.co',KEY='sb_publishable_6uNgrPI-Mj84tlHcFuICTQ_aG2zfoSU';const menu=[['Panoramica','/admin'],['Utenti','/admin/utenti'],['Progetti','/admin/progetti'],['Richieste consulenza','/admin/richieste'],['Abbonamenti e pagamenti','/admin/abbonamenti'],['Contratti','#'],['Immobili','/admin/immobili'],['Personale','/admin/personale'],['Documenti','/admin/documenti'],['Statistiche','/admin/statistiche'],['Impostazioni','/admin/impostazioni']];export default function Page(){const[d,setD]=useState<any>({subscriptions:[],contracts:[]});useEffect(()=>{(async()=>{const t=localStorage.getItem('sb-access-token'),r=await fetch(`${API}/rest/v1/rpc/admin_billing_overview`,{method:'POST',headers:{apikey:KEY,Authorization:`Bearer ${t}`,'Content-Type':'application/json'},body:'{}'});if(r.ok)setD(await r.json())})()},[]);const paid=d.contracts.filter((x:any)=>x.payment_status==='paid'),revenue=paid.reduce((a:number,x:any)=>a+(x.amount_cents||0),0);return <AdminGuard><main className="admin-shell"><Side/><section className="admin-main"><header className="admin-top"><div><span className="eyebrow">AMMINISTRAZIONE</span><h1>Abbonamenti e pagamenti</h1><p>Controllo dei piani, rinnovi, Stripe e incassi registrati.</p></div></header><section className="admin-kpis user-kpis"><K n={d.subscriptions.length} l="Abbonamenti" s="Piani registrati"/><K n={d.subscriptions.filter((x:any)=>['active','trialing'].includes(x.status)).length} l="Attivi" s="Piani operativi"/><K n={paid.length} l="Pagamenti" s="Contratti pagati"/><K n={euro(revenue)} l="Incassato" s="Totale registrato"/></section><section className="admin-panel requests-panel"><div className="users-toolbar"><div><span className="eyebrow">PIANI</span><h2>Abbonamenti registrati</h2></div></div><div className="admin-data-table"><div className="data-head"><span>CLIENTE</span><span>PIANO</span><span>CICLO</span><span>IMPORTO</span><span>STATO</span><span>SCADENZA</span></div>{d.subscriptions.map((s:any)=><div className="data-row" key={s.id}><span><b>{s.full_name||s.email}</b><small>{s.email}</small></span><span>{s.plan_name||s.plan_code}</span><span>{s.billing_cycle||'—'}</span><span>{euro(s.amount_cents||0)}</span><span><b className="status-pill">{s.status||'—'}</b></span><span>{date(s.current_period_end)}</span></div>)}</div></section><section className="admin-panel requests-panel admin-section-gap"><div className="users-toolbar"><div><span className="eyebrow">PAGAMENTI</span><h2>Contratti e incassi</h2></div></div><div className="admin-data-table"><div className="data-head"><span>CLIENTE</span><span>PRODOTTO</span><span>IMPORTO</span><span>CONTRATTO</span><span>PAGAMENTO</span><span>DATA</span></div>{d.contracts.map((c:any)=><div className="data-row" key={c.id}><span><b>{c.signer_name||'Cliente'}</b><small>{c.signer_email||''}</small></span><span>{c.product_name}</span><span>{euro(c.amount_cents||0)}</span><span>{c.status}</span><span><b className="status-pill">{c.payment_status}</b></span><span>{date(c.created_at)}</span></div>)}</div></section></section></main></AdminGuard>}function Side(){return <aside className="admin-side"><a href="/" className="admin-brand"><span>PROGETTO</span>ABITARE INSIEME<small>CONTROL CENTER</small></a><nav>{menu.map(([m,h],i)=><a className={i===4?'active':''} href={h} key={m}><span>{String(i+1).padStart(2,'0')}</span>{m}</a>)}</nav></aside>}function K({n,l,s}:any){return <article><span>{l}</span><strong>{n}</strong><small>{s}</small></article>}function euro(c:number){return new Intl.NumberFormat('it-IT',{style:'currency',currency:'EUR'}).format(c/100)}function date(v:any){return v?new Date(v).toLocaleDateString('it-IT'):'—'}