'use client';
import{useEffect,useMemo,useState}from'react';import AdminGuard from'../AdminGuard';import'../admin.css';
const API='https://lbkdoxpanxvsrgqimzzj.supabase.co',KEY='sb_publishable_6uNgrPI-Mj84tlHcFuICTQ_aG2zfoSU';const menu=[['Panoramica','/admin'],['Utenti','/admin/utenti'],['Progetti','/admin/progetti'],['Richieste consulenza','/admin/richieste'],['Abbonamenti e pagamenti','#'],['Contratti','#'],['Immobili','/admin/immobili'],['Personale','/admin/personale'],['Documenti','#'],['Statistiche','#'],['Impostazioni','#']];export default function Page(){const[d,setD]=useState<any>({properties:[],requests:[]}),[q,setQ]=useState(''),[open,setOpen]=useState<string|null>(null);useEffect(()=>{load()},[]);async function load(){const t=localStorage.getItem('sb-access-token');const r=await fetch(`${API}/rest/v1/rpc/admin_properties_overview`,{method:'POST',headers:{apikey:KEY,Authorization:`Bearer ${t}`,'Content-Type':'application/json'},body:'{}'});if(r.ok)setD(await r.json())}async function patch(table:string,id:string,data:any){const t=localStorage.getItem('sb-access-token');const r=await fetch(`${API}/rest/v1/${table}?id=eq.${id}`,{method:'PATCH',headers:{apikey:KEY,Authorization:`Bearer ${t}`,'Content-Type':'application/json'},body:JSON.stringify(data)});if(r.ok)load()}const rows=useMemo(()=>d.properties.filter((p:any)=>(p.title+' '+p.municipality+' '+p.province+' '+p.contact_name).toLowerCase().includes(q.toLowerCase())),[d,q]);return <AdminGuard><main className="admin-shell"><aside className="admin-side"><a href="/" className="admin-brand"><span>PROGETTO</span>ABITARE INSIEME<small>CONTROL CENTER</small></a><nav>{menu.map(([m,h],i)=><a className={i===6?'active':''} href={h} key={m}><span>{String(i+1).padStart(2,'0')}</span>{m}</a>)}</nav></aside><section className="admin-main"><header className="admin-top"><div><span className="eyebrow">MARKETPLACE IMMOBILIARE</span><h1>Immobili</h1><p>Gestisci annunci, disponibilità e richieste ricevute.</p></div></header><section className="admin-kpis user-kpis"><article><span>Immobili</span><strong>{d.properties.length}</strong><small>Annunci presenti</small></article><article><span>Attivi</span><strong>{d.properties.filter((x:any)=>x.status==='active').length}</strong><small>Visibili nel portale</small></article><article><span>Vendita</span><strong>{d.properties.filter((x:any)=>x.listing_type==='sale').length}</strong><small>Proposte in vendita</small></article><article><span>Richieste</span><strong>{d.requests.length}</strong><small>Contatti ricevuti</small></article></section><section className="admin-panel requests-panel"><div className="users-toolbar"><div><span className="eyebrow">CATALOGO</span><h2>Gestione immobili</h2></div><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Cerca immobile, città, contatto…"/></div><div className="market-admin-list">{rows.map((p:any)=><article className="market-admin-card" key={p.id}><div><span className="eyebrow">{p.listing_type==='rent'?'AFFITTO':'VENDITA'}</span><h3>{p.title}</h3><p>{p.municipality} ({p.province}) · {p.square_meters||'—'} m²</p><small>{p.bedrooms||'—'} camere · {p.bathrooms||'—'} bagni · {p.elevator?'Ascensore':'No ascensore'}</small></div><div className="market-price"><strong>{euro(p.asking_price)}</strong><small>{p.contact_name||'Contatto non indicato'}<br/>{p.contact_email||''}<br/>{p.contact_phone||''}</small></div><div className="market-controls"><select value={p.status||'active'} onChange={e=>patch('properties',p.id,{status:e.target.value})}><option value="active">Attivo</option><option value="pending">In revisione</option><option value="inactive">Non attivo</option><option value="sold">Venduto/Affittato</option></select><button onClick={()=>setOpen(open===p.id?null:p.id)}>Dettagli →</button></div>{open===p.id&&<div className="market-detail"><p>{p.description||'Nessuna descrizione.'}</p><p><b>Indirizzo:</b> {p.address||'—'} · <b>Venditore:</b> {p.seller_type||'—'} · <b>Accordo commissione:</b> {p.commission_agreement?'Sì':'No'}</p><p><b>Accessibilità:</b> {p.accessibility_notes||'—'} · <b>Spazio esterno:</b> {p.outdoor_space?'Sì':'No'}</p></div>}</article>)}</div></section><section className="admin-panel admin-market-requests"><span className="eyebrow">CONTATTI</span><h2>Richieste sugli immobili</h2>{d.requests.length?d.requests.map((r:any)=><div className="market-request" key={r.id}><span><b>{r.contact_name}</b><small>{r.email} · {r.phone}</small></span><p>{r.message||'Nessun messaggio'}</p><select value={r.status||'new'} onChange={e=>patch('property_requests',r.id,{status:e.target.value})}><option value="new">Nuova</option><option value="contacted">Contattata</option><option value="closed">Chiusa</option></select></div>):<p className="muted">Nessuna richiesta.</p>}</section></section></main></AdminGuard>}function euro(v:any){return v==null?'Prezzo su richiesta':new Intl.NumberFormat('it-IT',{style:'currency',currency:'EUR',maximumFractionDigits:0}).format(Number(v))}