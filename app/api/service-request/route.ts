import {NextResponse} from 'next/server';
const API='https://lbkdoxpanxvsrgqimzzj.supabase.co';
export async function POST(req:Request){try{const b=await req.json();if(!b.name||!b.email||!b.phone||!b.service_code)return NextResponse.json({error:'Dati mancanti'},{status:400});const key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!key)throw new Error('Missing service key');const notes=[`SERVIZIO: ${b.service_name} (${b.service_price})`,`Telefono: ${b.phone}`,`Città/Provincia: ${b.city||'-'}`,`Tipologia: ${b.structure_type||'-'}`,`Stato progetto: ${b.project_status||'-'}`,`Immobile: ${b.has_property||'-'}`,`Posti: ${b.beds||'-'}`,`Incontro: ${b.meeting_type||'-'}`,`Disponibilità: ${b.availability||'-'}`,`Progetto: ${b.message||'-'}`].join('\n');const payload={name:b.name,email:b.email,region:b.city||null,notes,source:`service_${b.service_code}`};let r=await fetch(`${API}/rest/v1/leads`,{method:'POST',headers:{apikey:key,Authorization:`Bearer ${key}`,'Content-Type':'application/json',Prefer:'return=minimal'},body:JSON.stringify(payload)});if(!r.ok){const fallback={name:b.name,email:b.email,notes,source:`service_${b.service_code}`};r=await fetch(`${API}/rest/v1/leads`,{method:'POST',headers:{apikey:key,Authorization:`Bearer ${key}`,'Content-Type':'application/json',Prefer:'return=minimal'},body:JSON.stringify(fallback)})}if(!r.ok)throw new Error('Database error');return NextResponse.json({ok:true})}catch{return NextResponse.json({error:'Invio non riuscito'},{status:500})}}
