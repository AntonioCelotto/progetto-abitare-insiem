'use client';
import Link from 'next/link';import {useSearchParams} from 'next/navigation';import {useState} from 'react';
const products:any={pro_monthly:{name:'Portale PRO',price:'39 € / mese',term:'Abbonamento mensile con rinnovo automatico.',kind:'subscription'},pro_yearly:{name:'Portale PRO annuale',price:'390 € / anno',term:'Abbonamento annuale con rinnovo automatico.',kind:'subscription'},assisted_monthly:{name:'Portale Assistito',price:'99 € / mese',term:'Abbonamento mensile con supporto continuativo.',kind:'subscription'},strategy:{name:'Sessione strategica',price:'250 €',term:'Prestazione una tantum di 60 minuti.',kind:'service'},light:{name:'Consulenza LIGHT',price:'2.900 €',term:'Percorso di consulenza da remoto.',kind:'service'},full:{name:'Consulenza FULL',price:'4.500 €',term:'Affiancamento operativo e 12 mesi di Portale PRO.',kind:'service'},'360':{name:'Abitare Insieme 360',price:'da 7.900 €',term:'Proposta personalizzata dopo valutazione del progetto.',kind:'quote'}};
export default function ContractPage(){const q=useSearchParams(),code=q.get('product')||'pro_monthly',p=products[code]||products.pro_monthly;const[accepted,setAccepted]=useState(false),[busy,setBusy]=useState(false),[msg,setMsg]=useState('');async function proceed(){if(!accepted)return;setBusy(true);setMsg('');try{const r=await fetch('/api/checkout',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({product:code,accepted:true,contractVersion:'1.0'})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Pagamento non ancora disponibile');if(d.url)location.href=d.url;else setMsg(d.message||'Richiesta registrata.')}catch(e:any){setMsg(e.message)}finally{setBusy(false)}}return <main style={{maxWidth:900,margin:'0 auto',padding:'48px 24px 80px',color:'#173d34'}}><Link href="/abbonamenti">← Piani e servizi</Link><p style={{letterSpacing:3,fontWeight:700,marginTop:48}}>CONTRATTO DIGITALE</p><h1 style={{fontFamily:'Georgia,serif',fontSize:'clamp(42px,7vw,72px)',margin:'12px 0'}}>{p.name}</h1><div style={{fontSize:28,fontWeight:700}}>{p.price}</div><p>{p.term}</p><section style={{background:'#fff',border:'1px solid #ded8ca',borderRadius:20,padding:28,marginTop:32}}><h2>Condizioni essenziali</h2><p>Il servizio è fornito attraverso il portale Abitare Insieme. Le funzionalità e le attività comprese sono quelle indicate nella pagina del piano scelto e nella presente conferma d'ordine.</p><p>Gli importi sono da intendersi + IVA ove applicabile. Costi di professionisti terzi, pratiche, lavori, forniture e trasferte straordinarie non sono inclusi salvo espressa indicazione.</p>{p.kind==='subscription'&&<p>L'abbonamento si rinnova secondo la periodicità scelta. La gestione del pagamento e del rinnovo sarà disponibile nell'area personale.</p>}<p>Il contratto accettato e lo stato del servizio resteranno consultabili nella sezione “Il mio abbonamento”.</p></section><label style={{display:'flex',gap:12,alignItems:'flex-start',margin:'28px 0',fontWeight:600}}><input type="checkbox" checked={accepted} onChange={e=>setAccepted(e.target.checked)} style={{marginTop:4}}/> Ho letto e accetto le condizioni del servizio e confermo la scelta di {p.name}.</label><button onClick={proceed} disabled={!accepted||busy} style={{border:0,borderRadius:10,padding:'16px 24px',background:accepted?'#285c4d':'#9badA7',color:'#fff',fontWeight:700,fontSize:16,cursor:accepted?'pointer':'not-allowed'}}>{busy?'Preparazione…':p.kind==='quote'?'Invia richiesta':'Accetta e continua al pagamento'}</button>{msg&&<p style={{marginTop:18,fontWeight:600}}>{msg}</p>}<p style={{marginTop:28,fontSize:13,color:'#68736e'}}>Prima della messa in produzione definitiva, il testo contrattuale completo potrà essere aggiornato con le condizioni legali e fiscali definitive della società fornitrice.</p></main>}
